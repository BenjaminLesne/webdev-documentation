- check eslint config
  => are all eslint plugin of all used library installed?

- check typescript config

- check knip run in pipeline

- check for “as any” in codebase

- do they use .vscode/settings.json with:

```json
{
  "typescript.tsdk": "node_modules/typescript/lib"
}
```
